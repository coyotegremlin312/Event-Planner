{
  "name": "event-planner",
  "version": "1.0.0",
  "description": "Find your favorite events!",
  "main": "index.js",
  "engines": {
    "node": "11.2.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "client": "cd client && yarn start",
    "dev": "concurrently \"yarn start\" \"yarn client\"",
    "heroku-postbuild": "cd client && yarn install && yarn run build"
  },
  "author": "Ashley B.",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^0.22.0",
    "concurrently": "^4.0.1",
    "express": "^4.16.4",
    "firebase": "4.2.0",
    "request": "^2.55.0",
    "request-promise": "^4.2.2",
    "simple-rate-limiter": "^0.2.3"
  }
}
